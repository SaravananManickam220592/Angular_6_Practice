import { CapitalizewordDirective } from './capitalizeword.directive';

describe('CapitalizewordDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizewordDirective();
    expect(directive).toBeTruthy();
  });
});
